import SwiftUI

struct ContentView: View {
    var body: some View {
        Text("Scriberman")
            .padding()
            .frame(maxWidth: .infinity, maxHeight: .infinity)
    }
}

#Preview {
    ContentView()
}
